{
  "private": true,
  "name": "minimalist",
  "engines": {
    "node": ">=10.21"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "web": "yarn workspace web",
    "server": "yarn workspace server",
    "dev": "concurrently \"yarn server watch\" \"yarn web start\"",
    "premigrate": "yarn server build",
    "migrate": ""
  },
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
